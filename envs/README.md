# envs/

Environment logic lives in `src/sparse_city/env.py` (`SparseCityEnv`).

This folder is reserved for future named scenario packs (still synthetic only).
Do not place Planet City exhibition assets, DTLA LIDAR, or real personal data here.
